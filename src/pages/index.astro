---
import Main from '../layouts/Main.astro';
import { makeProjectFromMarkdown } from "../types";
const projects = await Astro.glob("../pages/projects/**/*.md")
    .then((markdowns) => markdowns.map(makeProjectFromMarkdown));

console.log(projects);
---

<Main
    projects={projects}
>
